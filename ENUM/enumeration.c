//Enumeration or enum variable

#include<stdio.h>

enum bool{No=10,Yes=15};   // 0 1
int main()
{
enum point{x=2,y};  //acts as  2 + 1=> output =3
enum bool a;
a=Yes;
enum bool b;
b=y;


printf("\n a : %d",a);
printf("\n b : %d",b);

return 0;
}